The following integration point is currently disabled.

Integration Point: ${integrationPoint}

The following message delivery from Registry to ${sourceSystem} is not performed.

Payload: ${payload}


Actions: 

-For Registry Administrators: proceed with any manual action needed.