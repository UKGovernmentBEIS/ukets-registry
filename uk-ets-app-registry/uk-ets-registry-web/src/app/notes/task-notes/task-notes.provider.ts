import { InjectionToken } from '@angular/core';

export const TASK_NOTES_PARENT_DETAILS_PATH_TOKEN: InjectionToken<string> =
  new InjectionToken<string[]>('Parent base details path for Task Notes');
