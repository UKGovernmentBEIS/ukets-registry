export enum Paths {
  AccountList = '/account-list',
  RegistrationStart = '/registration/emailInfo',
}

export enum NavigationMessages {
  GoBackToTheAccountList = 'Go back to the account list',
  GoBackToRegistrationStart = 'Go back to and start the registration process again',
  GoBackDefault = 'Go back',
}
